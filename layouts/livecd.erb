<% content_for :sidebar_extra do %>
  <%= get_sidebar_capture('Reiser4-TrueCrypt liveCDs', 'LiveCDs dedicated to power users and system administrators. No GUI, reiser4&truecrypt enabled, with additional tools for offline system administration and disaster recovery.') %>
<% end %>

<%= yield %>
